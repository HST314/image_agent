# 平面设计 Agent 修复任务清单 v1.1（当前权威版本）

> 更新依据：v1.7.0 架构评审、《平面设计 Agent 产品与系统边界 v1.0》及 2026-08-07 已确认产品决策。
> 排序原则：P0 为数据、费用和主流程发布阻断；P1 为第一阶段核心闭环；P2 为可观测与操作能力；P3 为体验和工程质量。
> 范围说明：第一阶段主 Agent 进度与交付采用前端人工查看、手动回传；不建设自动通知/轮询集成，不建设完整无限画布。
> 进度基线：P0-01～P0-08 已在 `942aa4ae867ee134070c37eeaa18a3f943f4a8b8` 完成并通过独立验收；P1-01～P1-03 已完成独立验收，P1-04 以后未启动。

## P0 — 发布阻断：资产、费用、流程与一致性

- [x] **P0-01 持久化全部生成资产，彻底消除临时 URL 风险。** 候选生成、自动返工和人工微调完成后立即下载 Ark/TOS 图片，校验 HTTP 状态、MIME、大小和图片解码结果，写入 `ArtifactStore.save_bytes` 并计算文件 SHA-256。checkpoint、事件、交付只保存稳定 `artifact_id`/内部 URI/内容哈希；下载失败不得写成功态。提供既有临时 URL 的迁移/失效标记工具。验收：25 小时后所有历史图仍可展示、下载和校验。

- [x] **P0-02 统一资产引用与浏览器访问协议。** 定义 `artifact://<id>`（或等价稳定 URI），由受控 API 解析为项目资产响应；禁止向浏览器暴露服务器绝对路径。候选图、返工图、最终图、指导图和风格图统一走该协议。验收：不同来源资产均可显示，越权和路径穿越请求被拒绝。

- [x] **P0-03 将任务书人工确认接入付费生图硬门禁。** 生成可编辑 Markdown 任务书后必须进入正常等待态；确认事实绑定 `task_spec_version + subject_sha256 + actor + time`。任务书任何修改生成新版本并使旧确认失效，未重新确认不得调用生图供应商。验收：直接调用 API 也无法绕过门禁。

- [x] **P0-04 打通最终人工确认与交付冻结。** 增加“最终确认 / 继续修改”动作，确认绑定最终资产哈希；资产改变后旧确认失效。未确认是等待态而非 `failed_step`，拒绝或继续修改回到人工微调。确认后冻结本次交付，后续修改必须新建修订或分支并重新确认。

- [x] **P0-05 让 `runtime.yaml` 真正成为行为事实源。** 建立强类型 `RuntimePolicy`，启动时校验并生成项目级不可变配置快照/哈希；逐项接通 watermark、尺寸、候选并发、澄清预算、质检策略与轮数、重试、超时、流式行为等，删除业务代码中的重复常量。未知键、非法组合或声明但未接线的参数必须失败并给出明确错误。真实/离线模式在项目创建时固化，运行中不可切换。

- [x] **P0-06 修复事件写入、项目锁和崩溃恢复。** `EventStore.append` 使用线程安全单写入路径；项目锁具备线程所有权、多进程互斥、PID/启动时间或租约信息，并能安全识别失效持有者，避免崩溃后永久 423。验收覆盖 5 路并发、进程竞争、强制终止和恢复，JSONL 零损坏、事件不丢不重。

- [x] **P0-07 禁止 Skill 静默降级和伪生产。** 移除品类库/风格库的宽泛 `except: pass` 与生产路径硬编码 `offline_mode=True`。必需 Skill 加载失败时停止付费流程，记录结构化原因；只有策略明确允许时才可降级，并在 UI/trace 中醒目标识。

- [x] **P0-08 冻结最小主/子 Agent 契约。** 实现版本化 `DesignTaskEnvelope v1` 与最小 `DesignDeliveryEnvelope v1` 的 JSON Schema、运行时模型、正反例 fixture 和契约测试。入站原始任务不可修改；同一幂等键不得重复创建作业或付费调用。第一阶段交付仅向主 Agent 回传“最终图片 + 简短设计说明”，但内部仍保存最终确认与 trace 引用。暂不实现自动进度通知。

## P1 — 第一阶段核心闭环与可靠执行

> 状态：P1-01～P1-03 已完成独立验收；P1-04 以后未启动。

- [x] **P1-01 按已确认边界重构状态机。** 固定流程：接收任务 → 关键澄清 → Markdown 任务书 → 等待确认 → 品类分析 → 风格选择/VLM 解读 → 五图并发 → 等待选母版 → 自动质检返工 → 人工决策/微调 → 重新质检（可多轮）→ 最终确认 → 冻结交付 → 设计说明/手动回传。所有人工等待均是一等业务状态，不得记录为失败。

- [x] **P1-02 将长耗时调用改为异步作业。** `advance` 仅创建幂等 job 并返回 `202 + job_id`，worker 执行五图生成、质检与返工；提供状态查询及 SSE（或等价事件流），支持取消、断线重连、事件序号续传和 worker 重启恢复。刷新或超时不得导致重复付费调用。

- [x] **P1-03 把艺术风格库升级为图片型、可索引 Skill。** 每条风格至少包含稳定 `style_index`、参考图及哈希、简介、适用场景、风险/禁用场景和版本。Agent 必须基于任务书从目录选择 5 个不同条目，不得随机编造；输出选择原因、艺术理念、适配点、可借鉴机制和主要风险。缺项、哈希不符、重复、少于五项或命中禁用场景均在付费调用前阻断，风格库不得静默降级。（`09d9ac485f8f855b74a97f0527db4c3d94a29130` 已于 2026-08-07 通过独立验收：唯一索引漂移、零相关及合格不足五项均被拒绝，`allow_degraded` 下 VLM/生图调用计数均为 0；专项 `23 passed`，全量 `92 passed`。）

- [ ] **P1-04 实施风格方案 B：VLM 图转文，生图只用风格文字。** 五张风格图分别交给 VLM 产生结构化理解；五个候选各自注入对应文字和同一套内容、品牌、空间、合规硬约束。Prompt 明确只借鉴抽象视觉语言，不复制参考图主体、构图、文字、标识或独特表达。默认不把风格图直接送入生图模型；方案 A 仅保留为后续受控实验。

- [ ] **P1-05 校验“五种不同机制 / 五次独立生图”。** 对 `style_index` 唯一性、构图/材质/光影/叙事/图形语言差异、说明完整性和硬约束一致性做结构化校验；不足五种时不得假装完成。每个槽位使用稳定幂等键，部分失败只补失败项。

- [ ] **P1-06 为 VLM 输出增加 Schema 校验与一次定向修复。** 风格理解和视觉质检均先做结构化校验；解析失败或字段非法时把错误与原响应交给修复 Prompt 重试一次，仍失败则保存脱敏原始输出并进入可恢复错误，绝不使用伪造默认“通过”。

- [ ] **P1-07 完成自动质检上限后的人工分流。** 母版先自动质检/返工至通过或配置上限；到上限仍未通过时立即停止自动调用，展示未通过项，并允许人工选择“继续生成 / 人工微调 / 放弃”。不得自动挑最高分版本冒充通过。

- [ ] **P1-08 实现第一阶段圈画 + Prompt 多轮微调。** 仅支持矩形框选、自由画笔、颜色和粗细设置；把标注层与原底图合成为指导图，再连同自由文本 Prompt 交给图像编辑模型。每轮产生新资产和父资产引用，保留原图，旧质检自动失效并强制重新质检。不做蒙版、图层或完整无限画布。

- [ ] **P1-09 生成简短设计说明并支持手动回传。** 最终确认后依据任务书、风格选择和最终资产生成简短说明（设计理念、选择理由、任务适配点），与稳定最终图片一并形成最小 Delivery；前端提供明确的人工回传动作。说明生成失败可单独重试，不得损坏已确认资产。

- [ ] **P1-10 完善错误分类与恢复语义。** 区分超时、限流、鉴权、内容审核、结构化输出失败、不可重试输入错误和人工等待；记录每个候选槽位/返工轮次状态。retry 必须复用幂等键并从最后成功点恢复。

## P2 — 历史、分支、日志、设置与可观测性

- [ ] **P2-01 提供分支列表、当前分支和安全切换。** 切换只移动到已校验 checkpoint，不覆盖历史；查看历史不改变当前状态，从历史节点重新执行必须自动创建新分支并保留旧分支。

- [ ] **P2-02 建设可交互时间线。** 可只读查看任一历史节点的任务书、风格卡、候选图、质检、模型输出和资产；“从此处重开”需二次确认并清晰显示新旧分支关系。

- [ ] **P2-03 提供分页事件日志与真实进度。** 展示当前步骤、5 图完成数、返工轮次、重试及错误；后端提供游标分页和增量事件。进度来源于真实状态，不使用虚假计时；日志需脱敏密钥、令牌、个人信息和供应商原始响应。

- [ ] **P2-04 让所有模型调用可追溯。** 保存 Prompt/原始输出/解析结果、模型版本、配置哈希、父调用、耗时与错误；文本能力支持流式展示和断线续传，图片调用只展示真实状态事件。

- [ ] **P2-05 建设分级设置页。** 由运行时 schema 生成表单，显示默认值、作用域、风险和生效时机；普通操作者仅能修改业务参数，供应商、安全、费用上限仅管理员可改。密钥不可回显，修改需审计、版本化并只影响新 job 或明确的新分支。

- [ ] **P2-06 完善健康检查与诊断。** 检查模型路由、worker、队列、存储空间、事件写入和资产代理；对外不返回本地路径和底层异常，使用 `trace_id` 关联内部诊断。

## P3 — 前端体验与工程质量

- [ ] **P3-01 重构工作台信息架构。** 以前端监督台定位组织“只读原始任务 / 当前决策 / 实时进度 / 结果 / 历史”，移除生产态手填工程 ID 和完整任务卡；保留隔离的开发/离线调试入口。

- [ ] **P3-02 实现安全的 Markdown 任务书编辑与预览。** 保存源文、结构化版本和差异；严格清洗 HTML 防止 XSS，显示版本、修改人和确认状态。任务书编辑后立即提示旧确认失效。

- [ ] **P3-03 优化澄清问题交互。** 单选/多选项直接展示，提供推荐项、推荐原因、自定义输入、逐项校验和草稿保存；只询问影响结果的关键缺口。

- [ ] **P3-04 修复多尺寸图片展示。** 列表和详情使用自适应比例、`object-fit: contain`、缩略图、懒加载和可缩放预览，显示原始尺寸，避免裁切影响选图。

- [ ] **P3-05 拆分前端单文件并补齐可访问性。** 至少拆出 API client、状态/动作映射、任务书、风格卡、画廊、圈画编辑器、时间线和设置模块；统一加载/空态/错误态，支持键盘操作、焦点管理和读屏标签。

- [ ] **P3-06 建立自动化质量门。** 覆盖契约、状态迁移、确认门、runtime 参数接线、临时 URL、资产代理、并发/崩溃、异步 job/SSE、风格方案 B、五槽幂等、圈画合成、多轮质检以及浏览器主流程；P0/P1 用例失败时禁止发布。

## 建议实施批次

1. **批次 A（安全底座）**：P0-01～P0-08；先冻结契约和状态机，再解决资产、确认门、配置、并发与锁。
2. **批次 B（核心闭环）**：P1-01～P1-10；打通异步流程、图片型风格库方案 B、五图、质检、圈画微调和手动回传。
3. **批次 C（可操作性）**：P2 全部；交付分支、历史、日志、设置和诊断。
4. **批次 D（体验与质量）**：P3 全部；完成 UI 重构、可访问性与自动化发布门。

## 第一阶段发布红线

- [ ] 未确认或确认已失效的任务书，无法产生任何付费生图调用。
- [ ] 所有图片使用稳定 URI 与真实内容哈希，生成 25 小时后仍可回放。
- [ ] 五个候选分别可追溯到不同 `style_index`、VLM 解读、选择理由和独立调用。
- [ ] 风格参考图默认不直接进入生图模型；内容、品牌、空间与合规约束优先。
- [ ] 五图、质检和返工异步执行；刷新、断网、超时、worker 重启不会重复付费或永久 423。
- [ ] 自动质检达到上限会停止并交由人工决策；每轮人工微调后必须重新质检。
- [ ] 矩形/自由画笔标注可与底图合成指导图，并与 Prompt 一起完成多轮编辑。
- [ ] 历史只读，历史重开必建新分支；最终确认绑定资产哈希并冻结交付。
- [ ] 可人工向主 Agent 回传稳定最终图片和简短设计说明，不依赖内部目录或 checkpoint。
