# 平面设计 Agent 逐项实施规格参考 v1.0

> 配套文档：《平面设计 Agent 最新修复任务清单 v1.0》《平面设计 Agent 产品与系统边界 v1.0》  
> 文档性质：任务详解与派工、开发、验收的共同查阅依据；不替代产品边界，也不表示条目已完成。  
> 冲突优先级：已确认的最新产品决策 > 产品与系统边界 > 本文 > 修复任务清单中的摘要文字 > 临时任务描述。

# 使用说明

本文把清单中的每个 `Px-x` 从“路线图索引”展开为可执行规格，供调度、开发和验收共同使用。

1. **派工时**：先引用准确编号，再复制对应章节；补充本批基线 SHA/tag、责任人、依赖项、允许修改目录和交付时间。不得只把清单中的一句摘要交给执行者。
2. **开发前**：逐项确认“目标、不可突破规则、范围、非目标、数据/状态语义、验收场景”。如果现状与本文冲突，先记录差异并由架构/产品确认，不得自行改变产品含义。
3. **开发中**：以服务端不可绕过规则为硬约束；前端隐藏按钮、提示文字或 happy path 测试都不能替代后端门禁。涉及模型与付费调用时必须考虑幂等、重试、恢复、审计和失败停止。
4. **提测时**：开发者提交“条款—代码—测试—运行结果”映射，说明新增/修改文件、迁移影响、已知限制、回滚方法；不得只给截图或笼统声称“已完成”。
5. **验收时**：验收者按本章场景独立复现，至少覆盖正常、失败、重复、绕过、刷新/重启和历史兼容。P0/P1 任一硬门禁未满足即退回，不得附条件放行。
6. **变更控制**：若实施中发现本文未覆盖的产品歧义，在任务卡中列为“待决策”，暂停受影响部分。确认后同时更新产品边界、本文和测试，避免口头决策成为新事实源。
7. **完成标记**：本文不维护进度勾选。状态、提交和验收证据应记录在实施议题/发布清单；本文保持为稳定规格，只有规则变化时才升级版本。

建议每项交付证据统一包含：基线与最终 SHA、关键代码位置、Schema/迁移说明、自动测试命令及结果、至少一个负向用例、费用调用计数或幂等证据、需要人工操作时的复现步骤。任务可按依赖组合成小批，但每个编号必须独立给出验收结论。

## P0-01

**目标与问题。** 生图供应商返回的 Ark/TOS URL 是短期下载入口，不是资产。候选图、自动返工图、人工微调图一旦仅以 URL 写入 checkpoint，链接过期后历史、质检和交付都会失真。本项要建立“供应商结果只作为摄取源，受控资产库才是系统事实源”的规则。

**实施要求。** 每次供应商报告成功后，先下载字节并校验 HTTP 状态、重定向上限、MIME 白名单、最大体积、图片可解码性及像素上限；随后由 `ArtifactStore` 原子写入项目资产目录，以真实文件字节计算 SHA-256，生成稳定 `artifact_id`、内部 URI、格式、宽高、大小和来源审计信息。只有摄取成功才能把槽位/返工轮次写为成功。checkpoint、事件、质检输入和 Delivery 不再依赖供应商 URL；原始 URL 如为排障所需，只能脱敏、限期保留且不得作为展示入口。

**兼容与失败语义。** 下载、校验、落盘或元数据提交任一步失败，状态应为可重试的“资产摄取失败”，不得产生成功资产，也不得再次调用已经成功的供应商生成。重试优先复用供应商响应并重新摄取。为旧数据提供扫描/迁移工具：未过期的 URL 可补摄取，已失效的记录明确标记 `unavailable`，不能伪造本地资产或哈希。

**范围外。** 不在本项引入云对象存储、CDN 或图片编辑功能；存储后端可以保持本地，但接口必须允许未来替换。

**验收。** 验证三类生成结果都无长期外链；同一字节得到同一内容哈希；下载到一半、错误 MIME、超大文件、伪图片、磁盘写满均不写成功态；生成 25 小时后，刷新、历史回放、质检、下载和最终交付仍可用；对已成功生成但首次摄取失败的重试不会新增付费调用。

## P0-02

**目标与问题。** 后端文件路径、供应商 URL、`file://` 和浏览器 URL 混用会导致前端无法展示、目录泄露和越权。本项统一内部资产身份与对外读取协议。

**实施要求。** 定义稳定引用（推荐 `artifact://<artifact_id>`）及唯一解析器。浏览器只通过受控的项目资产 API 获取内容；API 根据已认证主体、项目归属和资产登记解析实际存储位置，返回正确的 `Content-Type`、长度、缓存/ETag，并支持安全下载。候选、参考、指导、返工和最终图使用同一 DTO，不返回服务器绝对路径。路径拼接必须在规范化后验证仍位于项目资产根目录。

**不可绕过规则。** 知道 `artifact_id` 不等于有权限；跨项目读取、`../`、编码穿越、符号链接逃逸、未登记文件和任意绝对路径都必须拒绝。资产哈希来自 P0-01 的真实字节，不以 URI 字符串代替。

**范围外。** 不要求本项完成全套用户认证体系，但资产服务必须有明确的授权钩子，并在当前可信本地模式下至少严格执行项目隔离。

**验收。** 各来源图片均能通过统一协议显示/下载；响应不泄露本地路径；不存在直接静态暴露整个项目目录；跨项目、穿越、符号链接和不存在资产得到明确 4xx；ETag/哈希与实际字节一致；前端不再解释多套 URI。

## P0-03

**目标与问题。** 任务书是用户允许付费生图的签字对象。生成了任务书不等于确认；按钮是否显示也不是门禁。本项把确认事实建成版本化领域数据并在所有付费生成入口强制校验。

**实施要求。** 任务书保存 Markdown 源文、结构化字段、递增版本和规范化内容 SHA-256。确认记录至少包含 `task_spec_version`、`subject_sha256`、decision、actor、actor_type、时间。编辑永远产生新版本，旧版本保留，旧确认只对原版本有效。进入候选生成前，由服务端 gate 校验当前任务书存在、确认决定为 approved、版本与哈希完全相符；批处理、retry、直接 API、恢复和内部调用都经过同一个 gate。

**状态语义。** 生成任务书后进入“等待任务书确认”，这是正常等待，不设置 `failed_step`。拒绝后仍停留在可编辑/待确认路径。确认动作应幂等：重复确认同一版本不产生多个有效事实；并发编辑与确认必须通过版本/乐观锁避免确认错对象。

**范围外。** 不建设多人会签或行政审批流；第一阶段是单一业务确认事实。

**验收。** 未确认、被拒绝、确认旧版本、哈希不符、确认后再编辑、伪造前端状态、直接调用生成 API 均无法触发供应商；正确确认后只允许当前版本继续；并发编辑/确认不会错绑；审计可回答谁在何时确认了哪一版具体内容。

## P0-04

**目标与问题。** 自动质检结束不代表任务完成，最终确认必须绑定用户实际看到的最终资产。人工等待不能被当作异常，确认后的交付也不能被静默覆盖。

**实施要求。** 增加“等待最终确认”状态和“最终确认 / 继续修改”领域动作。确认事实绑定当前最终资产 `artifact_id` 与真实内容哈希、actor 和时间；确认前校验该资产是当前分支最新可交付版本，并满足质检/人工例外规则。确认后生成不可变交付修订并冻结。任何图像变化都会使旧确认不适用于新资产；继续修改必须建立新修订或分支，重新质检并重新确认。

**状态语义。** 未操作是 waiting，不是 failed。选择继续修改回到人工微调入口；选择确认进入冻结交付。重复提交同一确认幂等；两个并发动作以状态版本控制，只能有一个生效。

**范围外。** 不在本项生成设计说明或自动回传主 Agent，这分别属于 P1-09。

**验收。** 无 `final_approved` 的正常流程停在等待态；直接 API 不能确认旧资产或越过必要质检；确认后原交付不可覆盖；修改产生新哈希并让旧确认失效；刷新/重启后等待和冻结状态一致；审计能从 Delivery 反查确认主体和资产字节。

## P0-05

**目标与问题。** 配置文件声明行为而代码另有硬编码，会造成操作员误判费用、安全与输出。`runtime.yaml` 必须通过唯一的强类型策略进入运行时。

**实施要求。** 建立 `RuntimePolicy` Schema，明确每个字段类型、默认值、范围、作用域、生效时机和冲突规则。启动加载时拒绝未知键、拼写错误、非法枚举和危险组合，并执行“已声明字段全部有消费点”的测试。项目创建时保存解析后的不可变配置快照、Schema 版本和哈希；后续 job 从项目快照读取，不再重新解释全局文件。接通 watermark、尺寸、候选数/并发、澄清预算、质检模式与上限、retry、timeout、流式等行为，移除重复常量。

**模式与权限。** 真实/离线模式创建时固化，恢复请求不能切换。影响供应商、安全和费用的参数不得由普通 job 请求覆盖。配置日志不包含密钥值。

**范围外。** 设置 UI 属于 P2-05；本项先保证后端事实源正确。

**验收。** 对每个配置字段做“改变值会改变对应行为”的参数化测试；未知/未接线字段启动失败；项目创建后修改全局 YAML 不影响旧项目；请求参数不能切换模式或绕开上限；快照和哈希可在 trace 中定位；watermark=false 等典型字段不再被代码常量覆盖。

## P0-06

**目标与问题。** 五路并发追加 JSONL、线程/进程竞争以及崩溃残锁会破坏审计或永久阻塞项目。本项统一事件串行化和可恢复互斥。

**实施要求。** `EventStore.append` 通过单一线程安全写入口，完整序列化一条记录后在锁内写入并刷新；多进程场景使用可靠文件锁或等价机制。事件拥有稳定 ID/序号，可检测重复和缺口。项目锁记录持有 PID、进程启动身份、取得时间、租约/心跳等足以辨别陈旧锁的信息；释放校验所有权，不能由其他线程误解锁。进程不存在或租约安全过期时可回收，活跃任务不得被抢占。

**恢复语义。** worker 崩溃后，从最后成功 checkpoint 和 job 状态恢复；进行中的供应商调用必须借助幂等键避免重复付费。损坏尾行应可隔离并诊断，不能默默吞掉整个历史。

**范围外。** 不要求本项引入分布式队列；若将来多机部署，应抽象锁后端。

**验收。** 5 路高并发反复写入后每行都是合法 JSON，事件 ID 不丢不重；线程、两个进程、错误释放、SIGKILL、PID 复用/陈旧锁均有测试；崩溃后可恢复且不会永久 423；活跃进程锁不会被误清理；恢复不会重复已完成槽位的付费生成。

## P0-07

**目标与问题。** 品类/风格 Skill 加载失败后静默继续，或生产路径强制离线，会把缺失能力伪装成成功并消耗费用。

**实施要求。** 移除宽泛吞错和生产硬编码 `offline_mode=True`。Skill Registry 返回明确的加载、Schema、版本、匹配结果；必需 Skill 缺失、损坏、版本不兼容或无合法匹配时，在首次付费调用前终止。只有 `RuntimePolicy` 明确允许且产品规则允许的降级才执行，并记录原能力、替代能力、原因、影响与醒目标识。生产/离线适配器由项目固化模式注入。

**错误语义。** 区分“无匹配”“文件损坏”“依赖不可用”“离线 fixture”，全部进入可解释、可恢复或不可恢复错误，而不是空列表后继续。trace 中不得把离线输出标为真实模型结果。

**范围外。** 风格库图片化属于 P1-03，本项只确保现有 Skill 失败安全。

**验收。** 删除/损坏品类库、风格库、索引或 Schema 时供应商调用数为零；生产模式确实走真实适配器；允许降级时 UI/trace 清楚展示；禁止降级时不能通过直接 API 绕过；不存在 `except Exception: pass` 类关键路径。

## P0-08

**目标与问题。** 平面设计 Agent 是受主 Agent 调度的子 Agent，需要稳定、幂等、可演进的输入输出边界，不能依赖人工填写内部任务卡或读取 checkpoint。

**实施要求。** 定义 `DesignTaskEnvelope v1` 和最小 `DesignDeliveryEnvelope v1` 的 JSON Schema、运行时模型、版本迁移策略、正反例 fixture 与契约测试。入站至少包含全局任务/设计作业 ID、幂等键、目标、稳定素材引用、品牌/空间/合规约束和版本；校验后的原始 Envelope 只读保存。相同幂等键+相同载荷返回同一 job；相同键+不同载荷明确冲突。出站包含最终稳定资产、真实哈希/格式/尺寸、简短说明、最终确认和内部 trace 引用。

**边界。** 前端只读展示原始任务，可编辑的是派生任务书。第一阶段采用人工查看、手动回传，不实现主 Agent 自动通知或完整进度协议。内部可保存丰富历史，但不强迫主 Agent理解内部状态结构。

**验收。** Schema 正反例、版本拒绝/兼容、素材稳定性、原始任务不可改、幂等重投、键冲突、并发登记、登记中崩溃恢复均有测试；相同请求不创建第二作业或重复付费；Delivery 无本地绝对路径和临时 URL，可独立验证资产哈希。

## P1-01

**目标。** 将已确认产品流程落实为唯一可执行状态机，使系统状态、前端动作和恢复语义一致。

**实施要求。** 建模以下阶段：接收任务、关键澄清、生成任务书、等待任务书确认、品类分析、风格选择/VLM 解读、五图生成、等待母版选择、自动质检返工、等待人工决策/微调、重新质检、等待最终确认、冻结交付、生成说明/手动回传。每个状态定义允许动作、前置 gate、输入输出、是否付费、重入/幂等和可达后继。人工等待均为一等状态；删除 YAML 与 Python 各自维护不同迁移关系的双重事实源，或通过生成/一致性测试保证唯一事实源。

**迁移。** 为旧 checkpoint 提供版本识别和显式迁移；无法安全映射时只读保留并提示，不得猜测。状态迁移与事件/checkpoint 原子关联。

**非目标。** 不在本项实现异步 worker、圈画 UI 或视觉美化，但状态机需为它们提供稳定接口。

**验收。** 覆盖完整正常链路、每个人工等待、拒绝/继续修改、质检到限、重复动作、非法跳转、重启恢复和旧数据迁移；等待态无 `failed_step`；任意付费状态均受 P0 门禁约束；前端可仅凭状态—动作映射决定可用操作。

## P1-02

**目标。** 将 30–90 秒的生图、质检和返工从 HTTP 请求生命周期剥离，页面刷新、超时和 worker 重启不再造成卡死或重复费用。

**实施要求。** `advance`/动作 API 只校验并创建幂等 job，返回 `202`、`job_id` 和查询位置。worker 按槽位/轮次执行，持久化 queued/running/succeeded/failed/cancel_requested/cancelled 状态、进度和心跳。提供查询 API 与 SSE（或等价流），事件带单调序号，客户端使用 Last-Event-ID 续传；断线可重新查询权威状态。取消是协作式的：停止未开始工作，已发出的供应商调用按结果安全收口。

**幂等与恢复。** 动作级、job 级和供应商调用级使用稳定幂等键。重复点击返回同一活动/已完成 job。worker 重启认领陈旧 running job，从最后成功单元恢复，不能重复生成已完成槽位。

**验收。** API 快速返回；慢调用不占用浏览器长请求；重复点击、客户端超时、刷新、SSE 断线、worker SIGKILL、重启和取消均有测试；进度与实际完成单元一致；任何场景不会重复付费或遗留永久锁。

## P1-03

**目标。** 把艺术风格库从文本占位升级为图片型、版本化、可索引 Skill，确保五种方向来自可追溯事实而非模型自由编造。

**实施要求。** 每条记录至少含唯一稳定 `style_index`、版本、参考图稳定资产/真实哈希、简介、适用场景、风险/禁用场景和可选标签。索引 Schema 校验引用完整性与唯一性；更新图片或语义时升级条目版本并保留历史可解析。选择器基于已确认任务书、品牌/空间/合规约束检索五个不同条目，输出选择理由、理念、适配点、可借鉴机制和风险。

**失败语义。** 合法匹配少于五项时停止并说明缺口，不能重复条目、临时编造或用 generic 填满。参考图必须经统一资产协议读取。

**验收。** 索引重复、图片缺失/哈希不符、Schema 错误被阻断；同一结果可追溯到库版本和五个条目；约束冲突条目不会入选；少于五个合法候选时无生图费用；历史项目仍可解析当时使用的版本。

## P1-04

**目标。** 落实风格方案 B：参考图仅供 VLM 抽象理解，候选生图只接收结构化风格文字，避免参考图抢占主体和品牌约束或诱发复刻。

**实施要求。** 对五个风格条目分别调用 VLM，输出固定 Schema：style_index、风格摘要、选择原因、艺术理念、视觉机制、任务适配点、禁止复刻元素、主要风险。候选 Prompt 组合“共同硬约束 + 对应一份风格解释”，明确硬约束优先，仅借鉴抽象语言，不复制主体、构图、文字、标识或独特表达。生图 payload 默认不得包含风格参考图字节/URL。

**审计。** 保存风格条目版本、VLM 输入资产哈希、结构化输出、最终 Prompt 版本和候选槽位映射。方案 A 如保留代码，只能是关闭的实验能力，不能被普通请求开启。

**验收。** 五个槽位各自对应一张参考图和一份解释；生图请求抓包/适配器测试证明不含参考图；五份 Prompt 共享内容、品牌、空间、合规约束且只有风格软约束不同；复刻禁止语明确；VLM 失败按 P1-06 处理且不进入生图。

## P1-05

**目标。** 保证交付给用户的是五种实质不同的艺术机制和五次独立、可恢复的生成，而不是换几个形容词、重复风格或部分失败后假装完成。

**实施要求。** 生成前校验五个 `style_index` 唯一；结构化比较构图、材质、光影、叙事、图形语言，设置最低差异规则；共同硬约束必须一致。五个槽位拥有稳定 slot ID、style_index、Prompt 哈希和供应商幂等键。并发批次允许部分成功，汇总状态明确区分成功/失败/待补；补跑仅针对失败槽位。

**失败语义。** 不足五种或说明不完整时回到风格选择/解释修复；不得复制成功图补位，不得自动换掉已确认的硬约束。单槽失败不丢弃其他成功资产。

**验收。** 重复 style_index、差异不足、硬约束漂移均在付费前被拒；五槽各有独立调用与审计；随机两槽失败后重试只新增两次调用；刷新/重启后槽位映射稳定；最终 UI 不把 3/5 显示成完成。

## P1-06

**目标。** 对风格理解和视觉质检的模型输出建立 Schema 校验与一次定向修复，避免格式波动终止流程或用默认值伪造通过。

**实施要求。** 保存原响应后进行稳健 JSON 提取、Schema/Pydantic 校验和领域校验。首次失败时，将脱敏原响应、明确错误路径和目标 Schema 交给专用 repair Prompt，仅重试一次；修复响应同样完整校验。仍失败进入可恢复结构化输出错误，保留调用关联和脱敏样本，等待重试/人工处理。

**不可突破规则。** 禁止 catch 后返回默认合格、默认高分或空解释；repair 不得重新解释任务并改变已合法字段；原始输出访问受权限和脱敏控制。

**验收。** Markdown 包裹、前后废话、缺字段、错类型、越界分数、未知枚举、语义矛盾均有测试；可修复样本第二次成功，不可修复样本明确失败；失败时生图/交付不继续；质检绝不因解析错误被标为通过；调用次数严格为首次+最多一次修复。

## P1-07

**目标。** 自动质检/返工达到配置上限后停止费用消耗，把真实未通过项交给人工决策，不能挑最高分版本冒充合格。

**实施要求。** 每轮记录输入资产、规则/模型版本、各检查项、分数/结论、返工 Prompt 和输出资产。达到通过条件立即停止；达到上限仍未通过则进入人工分流等待态并展示累计轮次、最新图和未通过项。允许动作仅为：继续生成（建立明确的新预算/轮次段）、人工微调、放弃。继续生成必须再次确认费用影响或符合已冻结预算策略。

**状态语义。** 到限不是系统错误，也不是通过。人工未选择前不得继续调用。放弃保留历史并以明确终止状态结束；人工微调进入 P1-08 后必须重新质检。

**验收。** 通过、首轮失败后通过、恰好到限、到限未通过、零/非法上限、重复人工动作均有测试；到限后的供应商调用数不再增长；UI 展示具体未通过项；继续生成遵守新预算和幂等；最高分但未达阈值仍标为未通过。

## P1-08

**目标。** 实现第一阶段最小可用的“圈画指导图 + 文本 Prompt”多轮人工微调，不扩张为完整无限画布。

**实施要求。** 编辑器支持矩形、自由画笔、颜色、粗细、撤销/清空和指导图预览。标注层以与原图一致的像素坐标保存；处理 CSS 缩放、设备像素比和不同宽高比。提交时服务端把标注层与原底图合成为不可变指导图，连同自由文本 Prompt、原资产引用和编辑参数发送图像编辑模型。每轮生成新资产，记录 parent_asset_id、guidance_asset_id、Prompt、actor、轮次和幂等键，绝不覆盖原图。

**门禁与安全。** 输入资产必须属于当前项目/分支；空 Prompt/空标注按明确规则校验；输出仍经 P0 资产摄取。任何新图让旧质检和最终确认失效，随后强制进入重新质检。重复提交同一编辑请求不得重复付费。

**范围外。** 不做多图层、对象移动、蒙版语义、套索、多人协作或完整无限画布。

**验收。** 横/竖/超宽图在缩放视图中的标注能准确落到原始像素；矩形、画笔、颜色、粗细和合成结果正确；多轮父子链完整、原图可回看；跨项目资产被拒；刷新恢复草稿/结果；新图旧质检失效并自动回到复检；重复提交不重复调用。

## P1-09

**目标。** 最终确认后生成可供主 Agent理解的简短设计说明，并与最终稳定图片组成最小 Delivery，第一阶段由人工明确触发回传。

**实施要求。** 说明只基于已确认任务书、最终采用的风格/选择理由和最终资产/质检事实，包含设计理念、选择理由、任务适配点，避免虚构未发生的设计过程。Delivery 引用 P0-08 契约，带稳定 URI、真实哈希、格式/尺寸、最终确认和 trace 引用。前端显示待回传/已回传状态及显式动作，回传记录 actor、时间、目标和 Delivery 版本。

**失败与幂等。** 说明生成是独立可重试步骤；失败不撤销、覆盖或损坏已确认资产。重复点击手动回传同一 Delivery 必须幂等；资产/说明变化形成新 Delivery 版本并需满足相应确认规则。

**范围外。** 不实现自动通知、轮询、Webhook 或把全部中间资产发给主 Agent。

**验收。** 说明可追溯且与最终图/任务书一致；说明失败可单独重试，最终图保持冻结；Delivery 不含临时 URL/本地路径；重复回传不创建重复记录；主 Agent只依赖契约即可取得图片和说明，无需读取 checkpoint。

## P1-10

**目标。** 建立统一、可操作的错误分类和恢复语义，让 retry 只重做必要单元，并向用户提供正确行动建议。

**实施要求。** 定义错误 taxonomy：超时、限流、鉴权、内容审核、网络/供应商、资产摄取、结构化输出、不可重试输入、配置/Skill、取消及人工等待。错误记录包含稳定 code、阶段、槽位/轮次、retryable、建议动作、trace_id 和脱敏详情。重试策略由 RuntimePolicy 控制，使用指数退避/Retry-After 和上限；鉴权、非法输入、内容政策等默认不盲重试。

**恢复。** checkpoint 保存每槽/每轮成功边界；retry 复用原幂等键，从最后成功点继续。人工等待从不进入错误分类。前端按 code 显示可重试、需修改输入、需管理员或需人工决策。

**验收。** 每类错误有适配器/状态测试；429、超时、401、审核拒绝、坏 JSON、坏输入行为不同且正确；部分成功仅补失败单元；重启后 retry 语义一致；日志无密钥和完整敏感供应商响应；等待态不会出现“重试错误”按钮。

## P2-01

**目标。** 提供可审计的分支列表、当前分支和安全切换，支持探索与返工但不覆盖历史。

**实施要求。** 分支有稳定 ID、名称、父分支、分叉 checkpoint、创建人/时间、当前 head 和状态。查看/切换只能指向已校验且属于项目的 checkpoint；切换改变当前工作上下文但不删除任何记录。历史节点“重新执行”必须新建分支，并复制必要的不可变引用而非改写旧分支。

**并发与门禁。** 活跃 job 期间按规则禁止切换或先安全取消；使用版本锁避免两个操作者同时移动 head。跨分支的任务书确认、质检和最终确认不得错误复用，除非绑定对象哈希完全相同且规则明确允许。

**验收。** 可列出并识别当前分支；无效/跨项目 checkpoint 被拒；普通历史查看不改变 head；从历史重开自动建分支，旧分支逐字节保留；并发切换有冲突提示；刷新/重启后分支关系一致。

## P2-02

**目标。** 建设只读、可交互的历史时间线，使用户能检查任一节点的事实，并明确区分“查看过去”和“从过去重开”。

**实施要求。** 时间线按领域事件/状态展示任务书版本、风格卡、候选、质检、模型输出摘要、人工决策和资产。选择节点创建只读历史视图，不复用当前编辑表单，不触发状态迁移。重开动作单独呈现、二次确认，预览将创建的新分支及失效的后续确认关系。

**数据。** 历史 DTO 按需加载详情，资产仍走统一权限代理；缺失/迁移失败记录明确标识，不用当前数据填充过去。

**验收。** 任意节点可复现当时内容；浏览多个节点不改变当前分支和状态；重开必建新分支并显示父子关系；历史任务书/图片不可编辑覆盖；越权资产不可见；大项目按需加载而非一次返回全部 snapshot/history。

## P2-03

**目标。** 提供分页事件日志与来源真实的进度，让用户知道后台正在做什么，而不是用动画或计时器伪装进展。

**实施要求。** 事件 API 使用稳定游标/序号分页并支持 since 增量；事件包含阶段、job、槽位、轮次、状态、时间和 trace_id。进度由可数工作单元计算，例如五图 `2/5`、质检 `第 2/4 轮`，不可按预计时长伪造百分比。前端同时展示概要进度和可展开日志，处理重复、乱序、断线续传。

**安全与规模。** 服务端统一脱敏令牌、密钥、个人信息、原始供应商载荷；限制分页大小和查询范围。读取日志不改变业务状态。

**验收。** 5 图部分成功、retry、返工到限、取消和恢复均呈现准确；游标无重复/遗漏，新增事件可增量取得；大日志不会全量载入；不存在虚假 99% 卡住；敏感样本经测试不出现在 API/UI。

## P2-04

**目标。** 让每次模型调用可追溯、可关联、可受控查看，同时正确区分文本流式 token 与图片任务状态。

**实施要求。** 调用记录包含能力、模型/供应商版本、Prompt 模板版本、最终 Prompt 或安全引用、输入资产哈希、配置哈希、父调用、开始/结束、耗时、状态、解析结果和错误。文本模型可流式保存带序号的增量并支持断线续传，最终以完整响应为权威；图片模型不伪造 token 流，只发布 queued/running/provider_completed/ingested 等真实事件。

**权限与保留。** 原始输入输出可能含敏感信息，默认展示摘要，详细内容需权限；按策略脱敏和保留。审计记录不能因 UI 删除而丢失。

**验收。** 从候选或质检结果可反查完整调用链和配置；文本断线后续传不重不漏，最终文本一致；图片仅显示真实状态；模型异常/repair 关联正确；密钥、签名 URL 和受限内容不在普通视图泄露。

## P2-05

**目标。** 建设由运行时 Schema 驱动、权限分级且生效语义清晰的设置页，避免 UI 与后端配置再次漂移。

**实施要求。** 后端公开不含秘密值的配置描述：字段、类型、默认值、范围、枚举、帮助、风险级别、作用域和生效时机。前端据此生成表单并做客户端辅助校验，服务端仍执行最终校验。普通操作者只改业务参数；供应商、认证、安全和费用上限仅管理员。修改形成审计版本，只影响新 job、新项目或明确创建的新分支，页面必须提示。

**秘密处理。** 密钥只允许“未设置/已设置/更新”状态，不回显原值，不写入普通事件或配置快照。项目固化的真实/离线模式不可在运行中切换。

**验收。** 权限矩阵、非法值、未知键、并发更新、旧项目不受全局修改影响均有测试；Schema 增字段后 UI 可呈现；危险设置需明确确认；秘密永不从读取接口返回；每次修改能追溯 actor、前后版本和生效范围。

## P2-06

**目标。** 提供安全、分层的健康与诊断信息，快速区分模型、worker、队列、存储和资产服务故障，又不泄露内部路径。

**实施要求。** 区分 liveness 与 readiness；检查模型路由配置、worker 心跳、队列滞留、存储可写/容量阈值、事件追加和资产代理。外部响应只返回组件状态、稳定错误码、时间和 trace_id；本地路径、堆栈、凭据和供应商原始异常仅进入受控内部诊断。

**行为。** 慢或昂贵探针缓存/限频，健康检查本身不得触发付费模型调用。局部退化应显示 degraded 并说明对业务影响，不把所有问题都映射为 500。

**验收。** 模拟各组件故障时状态准确；接口不暴露 projects_root、绝对路径或秘密；探针超时受控且不拖垮服务；trace_id 可关联内部日志；未配置真实供应商的离线环境不会被错误标为生产健康。

## P3-01

**目标。** 把前端定位为受主 Agent 下发任务后的人工监督台，而非重复的任务创建器。

**实施要求。** 信息架构围绕：只读原始任务、当前需要人工做的决策、实时进度、当前结果、历史/分支。生产入口从路由/任务上下文取得项目，不要求用户手填工程 ID 或完整任务卡；原始 Envelope 不可编辑。开发/离线调试入口独立隔离并醒目标识，不能混入生产主路径。

**交互。** 状态机提供哪些动作，界面才展示哪些动作；每屏突出一个主要决策并保留上下文。加载、等待、失败、空态和冻结态使用统一语言。

**验收。** 从主 Agent任务入口可直接进入正确工作台；生产用户无法修改原始任务或误进离线模式；刷新保持上下文；不同状态显示的动作与后端 allow-list 一致；桌面常用尺寸下核心任务无需在多个无关面板寻找。

## P3-02

**目标。** 提供安全的 Markdown 任务书编辑、预览、版本差异和确认状态，清楚表达“编辑会让旧确认失效”。

**实施要求。** 保存 Markdown 源文和结构化版本；预览采用白名单 sanitizer，默认禁用原始 HTML/脚本、危险 URL 与事件属性。展示当前版本、内容哈希摘要、修改人/时间、确认主体与状态。编辑保存前显示 diff；保存创建新版本，不覆盖旧版，并立即展示需重新确认。

**并发。** 采用版本号/ETag 防止覆盖他人修改；草稿与已保存版本分离。最终门禁仍由 P0-03 服务端执行。

**验收。** 常见 Markdown 正确渲染；script、onerror、javascript URL、恶意 SVG/HTML 无法执行；编辑确认后文档会使确认失效；版本历史和 diff 准确；两个页面并发编辑出现冲突而非后写覆盖；绕过 UI 仍受 gate 拒绝。

## P3-03

**目标。** 优化关键澄清交互，只问真正影响结果的缺口，并让用户容易理解推荐答案与后果。

**实施要求。** 单选/多选选项直接展示，不折叠核心选择；每题显示原因、推荐项及推荐理由，并允许“自定义”文本。按题型做必填、数量和格式校验，支持草稿保存、返回修改和键盘操作。已从材料可靠推断的事实直接展示为可核对结论，不重复提问；问题预算受 RuntimePolicy 控制。

**数据。** 答案绑定 question_id/schema/version，保留来源和用户覆盖事实；题目变更后不能错误复用不兼容答案。

**验收。** 单选、多选、自定义、必填、草稿、恢复和版本变化均正确；推荐不会自动替用户提交；只缺一个关键事实时不会生成无关长问卷；移动/桌面和键盘流程可完成；答案能稳定进入任务书且可审计。

## P3-04

**目标。** 完整、准确显示不同尺寸图片，避免裁切误导母版选择或圈画定位。

**实施要求。** 列表卡片保留比例并使用 `object-fit: contain`；详情提供适应窗口、1:1/缩放预览和原始宽高/格式/大小。使用后端缩略图或受控尺寸响应、懒加载和占位，原图按需加载。圈画编辑器共享同一坐标换算规则，不能用裁切后的视觉坐标提交。

**验收。** 方图、横图、竖图、超宽/超长图均不裁掉内容、不拉伸；缩略图与原图方向一致；加载失败有可恢复反馈；大量候选不会一次下载所有原图；缩放后圈画与 P1-08 原始像素准确对应。

## P3-05

**目标。** 拆分难以维护的前端单文件，建立可测试的模块边界与基本可访问性。

**实施要求。** 至少拆出 API client、状态—动作映射、任务书、风格卡、画廊、圈画编辑器、时间线、设置和公共反馈组件。业务状态来自统一 store/查询层，组件不自行猜后端语义。统一 loading/empty/error/disabled/toast，集中处理认证、trace_id、取消请求和错误映射。

**可访问性。** 所有动作可键盘完成；语义化控件、可见焦点、label、替代文本、对话框焦点锁定/返回、状态 live region 和合理对比度。不能仅靠颜色表达通过/失败。

**验收。** 模块有边界测试，状态—动作映射单一；关键流程键盘可完成且焦点顺序正确；读屏能读出图片、进度和错误；重复点击被安全禁用/幂等；拆分不改变后端产品规则且不引入第二套工作流。

## P3-06

**目标。** 建立覆盖关键产品红线的自动化质量门，防止修复后再次出现费用、资产、确认和恢复回归。

**实施要求。** 分层建设 Schema/契约单测、状态迁移与领域 gate 测试、存储/并发/崩溃集成测试、供应商适配器契约测试、API 测试和浏览器主流程。关键集合至少覆盖：主/子 Agent契约、任务书/最终确认、runtime 全字段接线、临时 URL 摄取、资产越权、事件并发与残锁、异步 job/SSE、风格方案 B、五槽幂等、VLM repair、到限分流、圈画合成、多轮复检、分支历史和 Delivery。

**质量要求。** 测试必须有可失败断言；付费边界用可计数 fake/沙箱验证调用次数；并发/崩溃测试可重复且不过度依赖 sleep；外部供应商真实沙箱 E2E 独立、受预算和密钥保护。P0/P1 测试失败时 CI 阻止 tag/release，不允许跳过后发布。

**验收。** 建立清单编号到测试文件/用例的追踪表；故意恢复典型缺陷时对应测试确实失败；CI 输出可复现命令、失败证据和制品；关键浏览器链覆盖刷新/断线/重复点击；发布产物能证明所有 P0/P1 门禁通过。

# 附录：任务卡最小模板

```markdown
# <编号> <标题>

- 基线 SHA/tag：
- 权威规格：本文件 <编号>；产品边界对应章节
- 依赖/阻塞：
- 目标与现状证据：
- 允许修改范围：
- 明确非目标：
- 服务端不可绕过规则：
- 数据/Schema/状态变化：
- 正常、失败、重复、绕过、恢复测试：
- 兼容/迁移/回滚：
- 交付证据：代码位置、测试命令与结果、调用计数、已知限制
- 独立验收结论：通过 / 退回（附证据）
```

