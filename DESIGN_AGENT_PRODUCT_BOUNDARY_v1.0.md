# 平面设计 Agent 产品与系统边界 v1.0

> 状态：待评审冻结  
> 依据：架构评审及 2026-08-07 产品决策回复  
> 用途：约束第一阶段实现、验收与后续 AI 编码，避免主 Agent、平面设计 Agent、人工监督台三者职责漂移。

## 1. 产品定位

本系统是受主 Agent 调度的平面设计子 Agent，不是独立的全局任务创建系统。

- 主 Agent 接收全局多模态材料，提取并下发平面设计子任务。
- 平面设计 Agent 负责澄清、任务书、风格选择、候选生成、母版选择、自动质检返工、人工微调、最终确认与交付总结。
- 前端是平面设计任务的人工监督台：展示过程、承载必要决策与人工干预，不重复创建主 Agent 已下发的任务。
- 第一阶段暂不建设主 Agent 的自动事件通知/轮询集成；人工在前端查看并手动触发回传。

## 2. 已确认的产品决策

1. 主 Agent 下发的原始任务是不可变事实。人工只能编辑平面设计 Agent 形成的任务书；全局目标变化时由主 Agent下发新修订。
2. Agent 优先基于现有材料推断，只对影响结果的关键缺口提问，并给出推荐答案。
3. 任务书确认是付费生图前的强制人工门禁。
4. 任务书修改必须产生新版本；已确认版本的确认事实只绑定原版本，新版本必须重新确认。
5. 五种风格必须来自艺术风格库的五个不同风格条目，且在内容、品牌与空间约束一致的前提下形成实质差异。
6. 人工选定母版后先进行自动质检返工，达到通过条件或轮数上限后进入人工微调；每轮人工微调后重新质检。
7. 自动质检达到上限仍未通过时停止自动调用，展示未通过项，由人工选择继续生成、人工微调或放弃。
8. 第一阶段圈画编辑只支持矩形框选和自由画笔，可调整颜色与粗细；标注层与原底图合成为一张指导图，再与修改 Prompt 一并送入图像模型。不做蒙版、图层或完整无限画布。
9. 查看历史不改变当前状态；从历史节点重新执行时创建新分支并完整保留旧分支。
10. 最终确认后本次交付冻结；继续修改必须建立新修订或新分支，并重新最终确认。
11. 第一阶段主 Agent 集成采用人工前端查看、手动触发回传，不实现系统间自动进度通知。
12. 第一阶段回传范围为最终图片与简短设计说明。
13. 真实/离线模式在项目创建时固化；普通操作者只可修改业务参数，供应商、安全和费用上限仅管理员可改。
14. 实施顺序为：先修 P0、冻结契约、打通正确状态机与异步进度，再进行 UI 美化；不建设完整无限画布。

## 3. “确认/审批”的准确含义

本文统一使用“人工确认”，不使用容易产生行政流程歧义的“审批”。

任务书人工确认指：操作者阅读并确认当前版本的任务书准确表达了目标、素材、品牌、空间、合规和交付要求，允许系统基于该版本发起付费生图调用。

确认事实至少记录：

- `task_spec_version`
- 任务书内容哈希 `subject_sha256`
- `decision`（approved/rejected）
- `actor_id`、`actor_type`
- `confirmed_at`

任务书正文、版本或哈希任一变化，旧确认立即失效。最终确认同理，必须绑定最终资产内容哈希；资产变化后不得沿用旧确认。

## 4. 艺术风格库边界与推荐链路

### 4.1 风格库形态

艺术风格库是一个可索引的 Skill。每个条目至少包含：

- 稳定且唯一的 `style_index`
- 风格参考图及内容哈希
- 简介与适用场景
- 可选的风险、禁用场景和版本信息

当前纯文本占位条目应迁移为“图片为主、元数据为辅”的正式条目。Agent 必须根据任务约束从索引中选择五个条目，不得随机或脱离目录自由编造风格。

### 4.2 第一阶段采用方案 B

正式链路为：

1. Agent 根据任务书及风格条目简介选出五个不同的 `style_index`。
2. 五张风格图分别注入 VLM，VLM 输出结构化风格理解。
3. Agent 为每个选择输出简短说明：选择原因、艺术理念、任务适配点、可参考的构图/材质/光影/图形语言及主要风险。
4. 每个候选生图任务只注入对应条目的结构化风格文字，不注入风格参考图；同时注入同一份项目内容、品牌、空间与合规硬约束。
5. 五个风格分别独立调用一次生图，共生成五张候选图。

推荐 B 的原因：它把参考图转化为可检查、可审计、可裁剪的软风格约束，降低图像条件抢占品牌、主体、版式等硬约束的风险，也避免模型复刻参考图的构图或具体内容。

VLM 输出不得写成泛泛描述，应使用固定结构：

```text
style_index / 风格摘要 / 选择原因 / 艺术理念 / 可借鉴视觉机制 /
与本任务的适配点 / 禁止复刻的具体元素 / 主要风险
```

生图 Prompt 必须明确：只借鉴抽象视觉语言，不复制参考作品的主体、构图、文字、标识或独特表达；任务书中的内容、品牌、空间和合规约束优先级更高。

### 4.3 方案 A 的后续位置

风格图直接注入生图模型不作为第一阶段默认能力。后续仅在供应商支持可控参考强度时作为实验开关，并通过固定测试集比较任务约束满足率、品牌一致性、相似度/复刻风险和图像质量；未通过验收不得进入默认生产链路。

## 5. 第一阶段标准状态机

```text
接收主 Agent 任务
→ 材料理解与关键澄清
→ 生成可编辑 Markdown 任务书
→ 等待任务书人工确认
→ 广告品类库分析
→ 艺术风格库选择与 VLM 解读
→ 五风格独立并发生成
→ 等待人工选择母版
→ 自动质检/返工循环
→ 等待人工决定或微调
→ 圈画指导图 + Prompt 微调
→ 重新质检（可任意轮）
→ 等待最终人工确认
→ 冻结交付
→ 生成简短设计说明并手动回传
```

所有“等待人工”的节点都是正常业务状态，不得写为失败。耗时模型调用必须由异步任务执行，前端通过轮询或 SSE 查看进度，不得用长时间同步 HTTP 请求阻塞页面。

## 6. 主 Agent 与平面设计 Agent 契约边界

### 6.1 入站任务契约

第一阶段仍应冻结 `DesignTaskEnvelope v1`，至少包含：全局任务 ID、平面设计作业 ID、幂等键、全局目标、平面设计目标、稳定素材引用、品牌/空间/合规约束和契约版本。

- 原始 Envelope 校验后不可覆盖。
- 同一幂等键重投不得创建第二个作业或重复产生付费调用。
- 禁止把会过期的供应商临时 URL 当作长期素材引用。
- 前端只读展示原始任务，不允许直接修改。

### 6.2 出站交付契约

虽然第一阶段用户可见回传内容只要求“最终图片 + 简短设计说明”，系统仍以最小 `DesignDeliveryEnvelope v1` 承载：

- `task_id`、`design_job_id`、状态与契约版本
- 最终图片稳定 URI、内容哈希、格式与尺寸
- 简短设计说明（理念、选择理由、适配点）
- 最终确认事实
- 内部 trace 引用（供系统审计，不要求主 Agent 展示完整日志）

这不等于向主 Agent返回全部中间图片或模型原始输出；详细历史保留在平面设计 Agent 内部，可按审计需要读取。

## 7. 前端边界

第一阶段必须提供：

- 原始任务只读视图；可编辑、可渲染 Markdown 的任务书及版本确认。
- 五个风格选择理由卡和五张候选图；图片完整适配不同尺寸。
- 母版选择、自动质检进度、未通过项和轮数上限后的人工决策。
- 矩形与自由画笔标注，颜色/粗细设置，指导图预览，以及文本修改 Prompt。
- 任务异步进度、事件日志、当前分支、历史查看、从历史节点创建新分支。
- 最终确认、交付冻结和人工回传动作。
- 设置权限分级；项目创建后不得切换真实/离线模式。

第一阶段明确不做：多图片、多图层、蒙版编辑、可移动对象、完整无限画布、主 Agent 自动通知、返回全部中间资产。

## 8. 数据与运行时红线

- 所有生图结果立即下载进入受控资产库并计算内容哈希，不长期依赖供应商临时 URL。
- `runtime.yaml` 必须通过唯一运行时策略对象实际控制行为；禁止同一参数在业务代码中另行写死。
- Skill 加载失败不得静默降级并继续产生付费调用。
- 五图并发事件写入必须线程安全。
- VLM 结构化输出必须经过 Schema 校验，并允许一次定向修复；仍失败则进入可解释错误状态。
- 项目运行模式、配置快照和模型版本必须写入 manifest/trace。
- 最终资产、任务书、确认事实之间必须以版本和内容哈希绑定。

## 9. 第一阶段验收标准

1. 未确认任务书时无法触发生图；修改已确认任务书后确认自动失效。
2. 五个候选均能追溯到不同 `style_index`、VLM 风格说明、选择理由和独立生图调用。
3. 生图模型默认不接收风格参考图，只接收结构化风格文字；硬约束始终高于风格软约束。
4. 生图、质检和返工均异步执行，刷新页面不会丢任务或永久锁死。
5. 自动质检达到上限后停止调用并给出三种人工决策；人工微调后会重新质检。
6. 矩形/自由画笔标注可调整颜色粗细，能与底图合成指导图并连同 Prompt 发起微调。
7. 历史查看不改变当前分支；从历史重开会创建新分支且旧记录可查。
8. 最终确认绑定资产哈希；修改后必须建立新修订/分支并再次确认。
9. 最终图片使用稳定 URI 和真实文件哈希，24 小时后仍可展示与回放。
10. 可人工触发向主 Agent 回传最终图片和简短设计说明；主 Agent 无需读取内部 checkpoint。

## 10. 尚属技术验证、非产品待决的问题

- VLM 对各类风格图的结构化解读稳定性及一次修复成功率。
- 各生图供应商对纯文字风格描述的还原质量。
- 方案 A 在可控参考强度下是否能显著提升质量且不降低任务约束满足率。
- 标注指导图在不使用真实蒙版时对局部修改范围的控制精度。

这些问题通过固定样例集和供应商沙箱验证，不阻塞 v1 边界冻结，也不得在验证前改变默认链路。
